import 'package:flutter/material.dart';

/// 🌫️ Backgrounds
const klBackgroundColor = Color(0xFFF5F7F9);

/// 🔵 Primary Colors
const klPrimaryColor = Color(0xFF46BAD9);

/// 🟠 Secondary Accent
const klAccentColor = Color(0xFFEC7914);

/// 🔤 Text Colors
const klTextDark = Color(0xFF3F3F3F); // Dark text (e.g., headings)
const klTextMedium = Color(0xFF595959); // Sub-headings / field labels
const klTextHint = Color(0xFF888888); // Sub-headings / field labels

const kCardSurfaceColor = Color(0xFFFFFFFF); // Card/Panel background

const kButtonColor = Color(0xFF43B6D6);
const kButtonTextColor = Colors.white;

/// 🟢🔴 Status
const kSuccessColor = Colors.green;
const kErrorColor = Colors.red;
const kWarningColor = Color(0xFFFFA726); // Orange warning/pending
